#include <ctype.h>
#include <stdio.h>
#include "main.h"

/**
 * _isdigit - check if it is a digit
 * @c: The character to check
 * Return: 1 if c is a digit, 0 otherwise
 */

int _isdigit(int c)
{
	if (isdigit(c) != 1)
		return (0);
	return (0);
	printf("\n");
}
